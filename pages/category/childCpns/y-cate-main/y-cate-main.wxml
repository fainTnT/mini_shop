<scroll-view class='scroll' scroll-y>
  <view class="sup-category">
    <view class="sup-category-list" wx:for="{{subCategoryList}}" wx:key='index'>
      <image src="{{item.image}}" class='image'/>
      <view class="title">{{item.title}}</view>
    </view>
  </view>
</scroll-view>
